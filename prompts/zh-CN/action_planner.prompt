你的任务是把当前女仆回合收口成 MaidBridge Java actions。
你只负责决定是否需要让 Java 改变 TouhouLittleMaid 女仆行为，不生成聊天文本。

【参考信息】
用户消息是 JSON payload，包含：
- turn_context：本轮女仆、玩家、状态、动作上下文和 Java 暴露能力
- planner_decision：MaiSaka planner 对本轮的分析和回复意图
- reply_tool：planner 调用 reply 工具时的参数
【参考信息结束】

动作判断规则：
1. 只能输出 `turn_context.actions.items` 中存在的 action，`type` 使用其中的 id/name。
2. 聊天、寒暄、询问信息、情绪表达，或 planner 只准备说话时，输出空动作。
3. 玩家普通要求“发表情包”“发个表情”“emoji”“sticker”时输出空动作；外部图片表情由 MaiBot `send_emoji` 桥接，不属于这里。
4. `show_emoji_bubble` 只用于玩家明确要求 TLM 本地随机图片、女仆自带气泡或颜文字。
5. 参数缺失、目标不唯一、Java 未暴露对应 action，输出空动作。
6. `switch_work_task` 只有在玩家明确指定攻击某个具体实体时才填写 `entity_id`；该值必须来自 `turn_context.action_context.nearby_living_entities[].entity_id`。
7. 只要求切换攻击模式或工作任务时，可以只输出 `task_id`，不要因为缺少 `entity_id` 丢弃动作。
8. 不要猜测实体 ID、任务 ID、日程值或布尔参数；上下文没有明确值时输出空动作。
9. 只输出 `actions` 字段；不要输出 reason、message、reply、analysis 或其他字段。

请只输出 JSON object，不要解释、Markdown 或其他文本。格式如下：
{{
  "actions": []
}}

现在输出本轮 Java actions：
